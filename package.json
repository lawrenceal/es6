{
  "name": "es6-test",
  "version": "0.0.1",
  "scripts": {
    "build": "babel src -d lib",
    "watch": "babel src -d lib"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  }
}
